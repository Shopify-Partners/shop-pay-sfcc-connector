const formatMoney = (money) => {
    return money;
};

const pad = (str, length) => {
    return str + Array(length).join(' ');
};

const encodeBase64 = (value) => {
    return value;
};

module.exports = {
    formatMoney,
    pad,
    encodeBase64
};
